'use strict';

/**
 * Listen to Room reconnection events and update the UI accordingly.
 * @param {Participant} participant - The Room you have joined
 * @param {function} updateRoomState - Updates the app UI with the new Room state
 * @returns {void}
 */